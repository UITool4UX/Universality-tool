# Changelog

Every change to a formula, architectural boundary, domain model, public interface, security rule, or registered assumption **must** have an entry here, with affected test vectors and the approval reference. Entries are append-only; they are never edited or removed.

Entry format:

    ## YYYY-MM-DD — <title>
    - Type: formula | architecture | domain | validation | security | assumption | documentation
    - Change: <what changed and why>
    - Affected test vectors: <TV ids or "n/a">
    - Approval: <who/what approved>

---

## 2026-08-28 — index-v6 design system integrated into the Streamlit app (presentation-only)

- **Type:** architecture (new UI-band module + one permitted import edge) + presentation + documentation
- **Change:** Per the explicit 2026-08-28 task ("I have now added `ui/design_system.py` … Please integrate this design system into the existing Streamlit application … design_system.py must remain presentation-only … Do not modify universality/ … Do not modify calculation logic … Connect its visual components to the EXISTING real calculation results"):
  - **`ui/design_system.py` (rewritten to satisfy the existing contracts; role unchanged — presentation-only):** (a) `import streamlit` removed — the security audit permits `streamlit` in `ui/app.py` only; `inject_css()` now *returns* the single `<style>` block and the composition root injects it. (b) The Google-Fonts `@import url(https://fonts.googleapis.com/…)` removed — a CDN/network call, forbidden by the app's security contract (no CDNs, no network calls); fonts are local only: the existing self-hosted Lora `@font-face` data-URI mechanism (moved from `ui/app.py`, activates only if `ui/fonts/lora-*.woff2` assets exist) + the reference's system fallback stacks. (c) All `:.2f` fixed-point formatting removed — the A6 contract (`tests/test_services.py::TestSingleRoundingLocation`) makes `universality/services.py::format_for_display` the **sole** rounding location; the `render_*` components now receive **display-ready strings** produced by `format_for_display`. The ring-gauge arc is driven by the raw 0–1 index via SVG `pathLength` + browser-side CSS `calc()` (geometry only — no Python arithmetic, rounding, or clamping; a value in the C1 tolerance band renders a faithful full arc, unclamped). (d) Every string argument of the renderers is `html.escape`d (defense-in-depth); per the app's pinned hostile-input contract, **user input (product name, parameter names) never enters an HTML context at all** — it renders only via `st.text` / `st.dataframe`, so the hero banner, ring gauge, and diagnostics are fed fixed copy and `format_for_display` strings only (no user data, no hard-coded results). (e) CSS extended with the application chrome in the index-v6 language (error summary / field error / ok-caption, skip link, progress line, placeholder, masthead, part/parameter cards via the existing marker-span `:has()` hooks, results band success/stale, the five-button inventory), keeping the documented reduced-motion and high-contrast media queries. The hero title is a styled `div`, not an `<h1>` — the page's heading structure (wordmark as the H1 role, Parts I–III as H2) is owned by the app and must not gain a second level-1 heading (ACCESSIBILITY.md §2).
  - **`ui/app.py` (composition root; no behavior change):** imports `ui.design_system`; the local `_font_faces`/`_CSS`/`_style_block` are replaced by `design_system.inject_css()`; Part III now renders the index-v6 components wired to **this outcome only** — hero live readout (overall figure + FAP/SAP/DAP + user-group gap), ring gauge (display tier via `score_to_tier`), group pins, the gap card (keeping the `USER-GROUP GAP` label and the D-UI-9 max/min sub-label), and the diagnostics row ("Widest user-group gap" — the same D-UI-9 ordering read + the F4 gap value; no new computation). **No result value is hard-coded anywhere.** Unchanged: all widget keys and input behavior, session-state helpers, `_run_calculation`, validation/error rendering (verbatim gate messages, fix hints, summary placement), the last-resort exception guard, the two-click Reset, the per-parameter tables, all normative copy.
  - **`scripts/security_audit.py` (contract entry — the audit's own prescribed flow: "Adding an edge is a contract change (this file + changelog + approval)"):** `ui.app` permitted edges gain `ui.design_system`; new entry `ui.design_system: (none)` (presentation-only, stdlib only, no project imports). Docstring edge list synced. No detector logic changed.
  - **`docs/architecture.md`:** §2 band file list, §5 module table (new `ui/design_system.py` row; `ui/app.py` row updated), §6 edge table (two rows), §10 test-count pin for `tests/test_ui_app.py` (19 → 20).
  - **`docs/DESIGN_SYSTEM.md`:** supersession note — structural/behavioral contracts remain binding; its palette/type/elevation tokens (flat-paper mood-board) are replaced by the index-v6 module's tokens (explicit user direction for this task).
  - **`.streamlit/config.toml`:** theme tokens aligned to the design-system palette (`--signal #2454E8`, `--paper #F5F7FB`, `--surface #FFFFFF`, `--ink #0E1A30`, font `sans`); `base = "light"` kept — the design system's Part A widget overrides require it.
  - The invoice-style parameter table was **not** implemented (deferred per task: "We will handle that separately") — the existing `st.dataframe` tables are untouched.
- **Tests added:** `tests/test_ui_app.py` +1 (`test_known_case_ui_072_fap_092_sap_072_dap_052_gap_040`): drives the real app through `evaluate()` with scale 50, one parameter (weight 1.0), scores 46/36/26, and asserts the displayed UI = 0.7200, FAP = 0.9200, SAP = 0.7200, DAP = 0.5200, group gap = 0.4000 — through the real calculation engine and the new components. No existing test was modified.
- **Tests executed:** full suite `.venv/bin/python -m unittest discover -s tests` → 292 tests, all passing (baseline before this entry: 291 tests with **2 failures**, both caused by the freshly added `ui/design_system.py` — the streamlit import and the `:.2f` A6 violation; both fixed here). `scripts/security_audit.py` over the repository → exit 0, "no findings".
- **Affected test vectors:** n/a (no formula, constraint, assumption, or public interface changed; TV1–TV7 unchanged; the public API remains the 31-name contract).
- **Approval:** explicit user task directive (2026-08-28) adding `ui/design_system.py` and integrating it, with the constraints honored above (universality/ untouched; calculation logic, validation, error handling, session state, tests, and security controls preserved; design_system.py presentation-only; no second calculation engine).

## 2026-08-26 — Security audit (application-security review); findings fixed; contract-owned machine enforcement implemented

- **Type:** security + architecture (implementation of contract-owned tooling) + documentation
- **Change:** Repository-wide application-security audit per the explicit 2026-08-26 task ("Act as an application security engineer. Do not add features. Audit the complete repository… For every issue provide SEVERITY/FILE/LOCATION/PROBLEM/EXPLOIT-SCENARIO/FIX/REGRESSION TEST. After fixes, rerun the complete test suite."). **Verified clean (no change needed):** no `eval`/`exec`/`pickle`/`yaml`/`subprocess`/`os.system`/dynamic-import use anywhere; no network calls (telemetry off by launch flag); no file writes and no export functionality (V15 lane reserved); no secrets in code/config; no debug mode; NaN/±∞/booleans rejected at the gate (V1/V2/V3/V11) and re-guarded by domain invariants, engine guards, and `format_for_display` (all test-pinned); hard input limits (100-char names/product, 100 parameters, scale 2–100 whole number, scores 1..scale, weights 0..1 summing to 1); XSS/HTML-injection — every `unsafe_allow_html` interpolation is `html.escape`d, user names render only via `st.text`/`st.dataframe` (framework-escaped), pinned by the hostile-input AppTest; error handling — controlled V-coded messages + fixed generic `ServiceError` copy, redacted logging (never message/args/source lines); config — no `.streamlit/config.toml` (safe defaults), headless server, telemetry disabled. **Four findings were fixed:**
  - **F-1 (LOW) — `ui/app.py`: no last-resort exception boundary (stack-trace exposure).** Any exception escaping the controlled flows (e.g. corrupted session state → `KeyError`/`ValueError` in `collect_raw`; `OSError` reading font files; a future render bug) reached Streamlit's default error display = full traceback (message + frames + source lines) in the browser. **Fix:** `main()` wraps the render path in a last-resort guard — `RerunException` (`st.rerun` control flow) is re-raised; any other exception renders only the fixed generic error state (E5), with the exception object deliberately never bound (nothing of it can be rendered). Micro-hardening: the Reset auto-disarm reads `reset_armed_at` via `.get(..., 0.0)` so a corrupted session cannot `KeyError` on every run. **Flagged contract decision (D-SEC-1):** the guard does not redacted-log from the UI band — redaction is owned by `universality.diagnostics` (Application band) and the UI may import the package public API only (§6); exposing `log_unexpected` in the public API would be a surface contract change (31 → 32 names) requiring explicit approval, so it was **not** done. Accepted observability trade-off, recorded here.
  - **F-2 (MEDIUM) — machine-enforced security controls missing (documented, not implemented).** `docs/architecture.md` §3/§6/§10/§12/§15 designate `scripts/security_audit.py` + `tests/test_security_audit.py` as the enforcement of the forbidden-construct list and the §6 import policy, but neither existed; the rules were enforced only by ad-hoc per-module AST scans with coverage gaps (no forbidden-construct scan of `ui/app.py`, no repository-wide import-policy scan, no third-party gate). **Fix:** the contract-owned tooling is implemented exactly per its §5 spec — stdlib `ast`/`pathlib`/`sys` only, never imports the project; checks: forbidden calls (`eval(`, `exec(`, `compile(`, `__import__(`, `input(`, `os.system(`, `os.popen(`), forbidden imports (`pickle`, `subprocess`, `importlib`), any `shell=True`, the exhaustive §6 edge table (fail-closed for unlisted modules; UI → package public API only; no third-party except the documented `streamlit` exception in `ui/app.py` and `tests/*`); emits findings; exit code = count. This implements a security control the architecture contract already owns (§15 "New: scripts/security_audit.py + tests/test_security_audit.py") — **no product feature added; no public API change (still 31 names)**.
  - **F-3 (LOW) — no dependency manifest (floating Streamlit install).** No `requirements.txt`/`pyproject.toml` existed; a fresh install would float Streamlit to latest while the suite is validated against 1.62.0 (floating behavior already caused a divergence finding this project — the `number_input` silent clamp). **Fix:** `requirements.txt` pins `streamlit==1.62.0` (the validated version; the only third-party dependency); README install line updated; the pin is regression-pinned by `tests/test_security_audit.py::TestDependencyManifest`. **Dependency review (pip-audit, 2026-08-26):** Streamlit 1.62.0 and all runtime transitive dependencies — **zero known CVEs**. Only the venv's own build tooling (pip 23.0.1, setuptools 66.1.1 — not app runtime dependencies; the venv is ephemeral, never committed) carried advisories; the sandbox venv was upgraded in place.
  - **F-4 (LOW) — `log_unexpected` context not shape-enforced (log-injection vector, defense-in-depth).** `context` was documented as "an internal call-site identifier; callers must never pass user data in it" by convention only; a malformed context (e.g. containing `\n`) could forge log lines. **Fix:** fail-closed enforcement — `context` must match `[A-Za-z0-9._-]{1,64}` or is replaced by the fixed placeholder `<invalid-context>` before logging; the two current call sites (`evaluate.validation`, `evaluate.outcome`) are unchanged in behavior.
  - **F-5 (INFO) — no `.gitignore`:** `.venv/` (a full vendored dependency install) and `__pycache__/` were untracked and unignored — an accidental-commit / supply-chain hygiene risk. **Fix:** minimal `.gitignore` (`.venv/`, `__pycache__/`, `*.pyc`).
  No formula, constraint, default, or public interface changed (public API remains the 31-name contract); F1–F4, the domain, `validation.validate`, and `services.evaluate` are untouched.
- **Tests added:** `tests/test_security_audit.py` (7: audit tool exists and is stdlib-only; the real repository tree is clean (exit 0, "no findings"); usage error on missing root (exit 2); clean fixture tree clean; **negative controls — 11 planted violations** (`import pickle`, `eval(`, `exec(`, `__import__(`, `os.system(`, `shell=True`, third-party `requests`/`numpy`, `domain`→`services` edge, UI→`universality.validation` submodule edge, unlisted `universality/insights.py` module, `scripts`→project import) each reported with the matching rule; `requirements.txt` pins exactly `streamlit==1.62.0` as its single line); `tests/test_ui_app.py` (+2: unexpected `RuntimeError("SECRET-INTERNAL-DETAIL-42")` escaping through `universality.evaluate` → no framework exception, `service_error` state set, generic message shown, no class name / message / forbidden token rendered; `st.rerun` control flow (Add parameter) not treated as a fault); `tests/test_diagnostics.py` (+2: newline-laden context replaced by `<invalid-context>` — never logged verbatim, no injected line, exception class still reported; non-string context replaced).
- **Tests executed:** full suite `.venv/bin/python -m unittest discover -s tests` → **291 tests, all passing** (58 domain + 40 calculation + 69 validation + 14 integration + 9 property + 25 edge-case + 20 services + 11 diagnostics + 19 ui_model + 19 ui_app + 7 security_audit). System Python without Streamlit: 291 tests, 19 skipped (AppTest), all passing.
- **Affected test vectors:** n/a (no formula, constraint, or calculation change; TV1–TV7 unchanged).
- **Approval:** explicit user task directive (2026-08-26): "Act as an application security engineer. Do not add features. Audit the complete repository… After fixes, rerun the complete test suite."

## 2026-08-26 — APPLICATION layer (services, diagnostics) + UI band implemented

- **Type:** architecture + implementation (application + UI) + documentation
- **Change:** The remaining deferred layers landed per the explicit 2026-08-26 task ("Now implement the UI based ONLY on the approved design documentation. Do not redesign the product while coding. Do not change mathematical logic."), conforming to the four approved design contracts (`docs/DESIGN_SYSTEM.md`, `docs/UI_ARCHITECTURE.md`, `docs/UX_FLOW.md`, `docs/ACCESSIBILITY.md`):
  - `universality/services.py` (new): `evaluate(raw) -> EvaluationOutcome` — the single UI↔computation boundary: `ValidationRejection` propagates **unchanged**; any other exception is logged redacted and surfaced as `ServiceError` (frozen dataclass, `str()` = fixed generic message "Something went wrong. Please try again." — no internals ever escape). `format_for_display(value) -> str` — the **sole** A6 presentation-rounding location (fixed 4-dp string; rejects bool/str/None/non-finite; no clamping; never feeds back into computation).
  - `universality/diagnostics.py` (new): `logger` (`"universality.diagnostics"`, default `NullHandler`, `propagate = False`), `redacted_error(exc) -> dict` (class chain only — never the message/args), `log_unexpected(context, exc)` (fixed ERROR template: context identifier + class + class chain + cause/context flags + **safe stack frames** `file:line:function`; never the exception type/message line, never source lines — per the §9 redaction rule; `context` is an internal call-site identifier, never user data).
  - `universality/__init__.py`: public surface **25 → 31 names** (added `ServiceError`, `evaluate`, `format_for_display`, domain constants `SCALE_MIN`, `SCALE_MAX`, `PARAMETER_MAX`). `tests/test_integration.py::TestPublicSurface` updated to the 31-name contract.
  - `ui/app.py` + `ui/ui_model.py` (new): the single-page Streamlit composition root — all ten requested elements implemented (product setup, parameter creation, weight input, FAP/SAP/DAP input, validation feedback, calculate action, results, parameter analysis, group comparison, diagnostics surface) with the normative `UX_FLOW.md` §7 copy. `ui/ui_model.py` is pure assembly/label-mapping — **AST-enforced**: zero arithmetic-operator nodes, forbidden-call scan, import whitelist. Every user string is `html.escape`d before any HTML context; `unsafe_allow_html` only for static structure + escaped content; no CDNs, no network. `ui/fonts/README.txt` documents the four expected Lora asset filenames.
  - **Flagged framework finding (regression-pinned):** Streamlit 1.62 `number_input` **silently clamps** a stored session value that falls outside its bounds (empirical: scale 10 with p0_fap = 8, then scale → 3 made the stored value 1) — a silent modification of user data, which `UI_ARCHITECTURE.md` §4 forbids. Fix: score inputs use `max_value = SCALE_MAX` (100) instead of the current scale; the validation gate remains the authority and surfaces an orphaned score as V4 on exactly that field (flow E3). The E3 AppTest pins the stored value unclamped.
  - **Flagged render-order fix:** values stored during a button click (Calculate, two-click Reset) render above the button in script order, so they were only visible on the next interaction; `st.rerun()` now ends `_run_calculation()` and the reset arm/confirm branches (same pattern as Add/Remove).
  - **Flagged environment constraint:** the sandbox egress proxy blocked all font CDNs (fonts.googleapis.com, cdn.jsdelivr.net, unpkg.com), so the Lora woff2 assets could not be fetched; the app degrades to the documented fallback stack (D-UI-1) and `_font_faces()` will pick up the assets automatically if they are added to `ui/fonts/`.
  - **Flagged API note:** Streamlit 1.62 deprecates `use_container_width` (removal announced for 2025-12-31); the UI uses the current `width="stretch"` API (verified warning-free under `-W error::DeprecationWarning`).
- **Tests added:** `tests/test_services.py` (20: `evaluate` vs direct pipeline with canonical TV7 strings; rejection propagation unchanged (V11/V16/V6 verbatim/V4 field); unexpected-failure mapping via mocked internals carrying "SECRET" payloads → `ServiceError` + exact generic message + no leakage; 13-raw adversarial battery; `format_for_display` table + rejections; **TestSingleRoundingLocation** — repository scan proving the only rounding construct in the codebase is in `universality/services.py`); `tests/test_diagnostics.py` (9: redacted structure only incl. chained exceptions; safe stack frames logged for raised exceptions (file:line:function, no source-line/message text), none for unraised; context + class in message, ERROR level, deterministic template, `NullHandler` + `propagate False`); `tests/test_ui_model.py` (19: fresh-state defaults; `collect_raw` exact schema/key order/no-unknown-keys; `parse_field` 15-case table; `human_label` 12 verbatim labels; fix hints; COPY table; **AST purity: zero BinOp nodes, forbidden-call scan, import whitelist, forbidden-identifier scan**); `tests/test_ui_app.py` (17, AppTest: empty state + five-button inventory + visible labels + skip link; TV7 happy path with canonical strings + gap sub-label; result replacement; stale results dimmed; V16/V6/V8/V4-orphaned (E3, unclamped-value pin)/V4-range error states with verbatim message + translated label + fix hint; no-traceback / hostile-input scenario; Add/Remove/Reset-weights builder actions; two-click Reset; Add disabled at 100).
- **Tests executed:** full suite `.venv/bin/python -m unittest discover -s tests` → **280 tests, all passing** (58 domain + 40 calculation + 69 validation + 14 integration + 9 property + 25 edge-case + 20 services + 9 diagnostics + 19 ui_model + 17 ui_app). System Python without Streamlit: 280 tests, 17 skipped (AppTest), all passing.
- **Affected test vectors:** TV1, TV7 (canonical strings through the UI: overall 0.5533, FAP 0.8200, SAP 0.5900, DAP 0.2500, gap 0.5700); E2 (weight-sum error via UI), E3 (orphaned score after scale change via UI).
- **Approval:** explicit user task directive (2026-08-26): "implement the UI based ONLY on the approved design documentation", including the post-implementation verification checklist (tests, responsive, keyboard, contrast, error/empty/calculation/result states).

## 2026-08-26 — UI design contract created (design only — no UI code)

- **Type:** documentation (UI design)
- **Change:** Four binding design documents for the deferred UI band, produced per the explicit 2026-08-26 task ("Design the single-page application architecture. Do not implement the final UI yet"). Product framing: a **research data sheet** ("I already did my research. I just need to enter my findings.") — single page, document flow (masthead → Part I setup → Part II parameters → Part III results → footer), one action (Calculate), academic/calm tone.
  - `docs/DESIGN_SYSTEM.md` — visual contract: white/blue palette (paper `#F7F8FA`, cards `#FFFFFF`, blue washes `#EAF1FB`/`#D7E4F7`, brand `#1D4ED8`), Lora (self-hosted, SIL OFL) body + Baskerville display, 4–7px radius only, **zero shadows** (borders + color blocking carry hierarchy), 24px graph-paper grid at **6% brand-blue opacity** in the margins only, no glassmorphism/pills/neon/gradients/SaaS-dashboard patterns; full type scale; exactly-five-button inventory (Calculate primary / Reset evaluation secondary / Add parameter, Reset weights 1/n, Remove × tertiary); input states (default/focus/**no per-field success**/error/disabled); motion budget (200ms fade, reduced-motion honored); 5-icon set.
  - `docs/UI_ARCHITECTURE.md` — structure contract: UI = composition root, **no math** (no formula/tolerance/sum/round/coerce; only numeric literals are display conveniences), imports public API + `services` only, Streamlit mapping table with **honesty/fragility ratings**, session-state key contract, `collect_raw` as pure assembly, error-path → component mapping table (normative), the Application-layer surface the UI requires (`evaluate`, `format_for_display`), safety rules (unescaped-user-string ban — product names may legally contain markdown/HTML characters, `unsafe_allow_html` only for static style/escaped table, no CDNs), parameter-card grid, results structure, UI test strategy.
  - `docs/UX_FLOW.md` — behavior contract: non-goals (no persistence/wizard/live-recalc/i18n), form state machine (FRESH→EDITING→CALCULATING→SUCCESS|ERROR), event table with an explicit no-silent-modification column, happy path, error flows with verbatim gate strings, **normative copy table** for every visible string, loading/success/empty states.
  - `docs/ACCESSIBILITY.md` — WCAG 2.1 AA: exact heading outline + landmarks + skip link, label/`aria-invalid`/`aria-describedby` rules, focus management (error → summary panel; success → results heading), 3 live regions only, measured contrast table (all ≥ 4.5:1; disabled pair documented as exempt), 44px targets, 200% zoom, reduced-motion, keyboard map, and a manual acceptance checklist over the two reference scenarios (TV7 happy path; E2 weight-sum error).
  - **Flagged design decisions D-UI-1…D-UI-9** (register in `UI_ARCHITECTURE.md` §11): (1) Baskerville = system font stack (no licensed web font file exists), Lora self-hosted; (2) no per-field success state — section-level success only; (3) Streamlit + best-effort CSS overrides per the finalized architecture, with **Option B (vanilla SPA) documented as an unapproved change-control escape hatch** if CSS-override fragility proves unacceptable at implementation time; (4) visible editable defaults (scale 5, weight 1.0, scores 1 — number inputs cannot be blank); (5) new parameter gets weight 0.0, **never auto-redistributed** (no silent modification of entered data; 1/n offered via an explicit button); (6) **no parameter reordering** (entry order = deterministic summation order = display order); (7) no live weight total in baseline (static hint + post-Calculate error; a live total would need a new Application-layer helper — additive change control); (8) group legend once, full accessible names always; (9) gap max/min sub-labeling is the UI's only non-formatting operation (a `max`/`min` over already-computed values, labeling only).
  - **No UI code was written**; `ui/` does not exist. No formula, constraint, assumption, or public interface changed. `architecture.md` §16 updated to reference the design contract.
- **Tests added:** n/a (documentation task).
- **Tests executed:** full suite re-run to confirm no drift: 215 tests, all passing.
- **Affected test vectors:** n/a.
- **Approval:** Explicit user task directive (2026-08-26) defining the UX goal, tone, visual requirements, component/state coverage, and the four deliverables.

## 2026-08-26 — QA audit; integration / property / edge-case test layer added (test-only scope)

- **Type:** testing + documentation
- **Change:** Senior-QA audit of the implemented layers (domain, calculation, validation) against the binding documents, plus the test layers the audit identified as missing: **integration tests** (raw → `validate` → `compute` → `EvaluationOutcome` through the public API), **property-style invariant tests** (seeded pseudo-random samples; stdlib only — no property framework is installed), and **edge-case tests** (boundaries, extreme valid values, pinned IEEE-754 behaviors). **Audit result: no behavioral defect found** in `domain.py`, `calculation.py`, or `validation.py` — all formulas F1–F4, constraints C1–C5, rejection rules V1–V21 (V15 reserved), and documented bit-exact identities reproduce as specified. Six **stale documentation claims** (implementation described as deferred/nonexistent after it shipped) were fixed as documentation defects: `FORMULA_SPECIFICATION.md` status line + "future implementation" wording; three `(deferred)` docstrings in `universality/domain.py` (docstrings only — zero behavior change); `CALCULATION_ENGINE.md` out-of-scope list ("validation.py, deferred"); `architecture.md` status line (now includes VALIDATION); `tests/CALCULATION_TEST_VECTORS.md` test-count footer. **New canonical vector TV7** registered in `FORMULA_SPECIFICATION.md` (Accessible chair reference case: scale 10, weights 0.7/0.3, hand-verifiable terminating decimals, UI = 83/150, gap 57/100) — user-constructible (passes the A20 gate) and mapped to the integration tests; the first vector exercised end-to-end through the user-input pipeline. No formula, constraint, default, or public interface changed; no product feature added; production behavior untouched.
- **Tests added:** `tests/test_integration.py` — 14 tests: public-API surface contract (exactly the 25 documented names); TV1 pipeline (documented bit-exact identities `UI_F == 0.8`, `UI == 0.6`, `gap == 0.4` + tolerance-compared SAP/DAP); TV7 pipeline (41/50, 59/100, 1/4, 83/150, 57/100 within 1e-12; weight traceability; product metadata); TV5 cross-layer consistency (A20 gate rejects the user attempt at `parameters[0].scores.dap` with V4; engine-level construction computes the canonical 0.623/0.64); V6 end-to-end (0.96 → error, nothing computed); rejection determinism; 10-run bit-identical pipeline; unknown keys ignored end-to-end (bit-identical); Simple Mode group-weight roundtrip; `EvaluationOutcome` pairing consistency + mismatched-order rejection (DomainInvariantError); 6-case no-internal-exposure subset; int-vs-float score equivalence (bit-identical). `tests/test_properties.py` — 9 tests, 200 seeded samples (seed 20260826, reproducible): range invariants `0 ≤ UI_F/UI_S/UI_D/UI/gap` with the documented C1/C2-tolerance upper bounds (`1+EPSILON` / `1+2·EPSILON` / `1+EPSILON`) and per-parameter normalized/contribution bounds; C1 parameter-weight sum + bit-exact weight round-trip; C2 group-weight sum; **score independence** (changing parameter 0's FAP score leaves every other parameter's normalized + contributions bit-identical, ≥100 executed samples); **weight locality** (changing weights 0 and 1 leaves parameters ≥2 bit-identical; touched contributions exactly `new_w × s_norm`; group index == left-to-right sum of stored contributions bit-exact; F3 in documented order, ≥60 executed samples); score monotonicity (raising a score never decreases its group index or the overall; untouched indices bit-identical); repeatability (50 double runs bit-identical; two same-seed generators reproduce identical raws and results); shape/order/storage (names, order, product, scale, bit-exact score storage). `tests/test_edge_cases.py` — 25 tests: empty input (`{}` → V16; product-only → V7 scale order); minimal valid evaluation (scale 2, UI 0.5); single-parameter weight at/just outside the EPSILON boundary (inclusive bound pinned: 1e-9 accepted, 2e-9 rejected); scale-2 exact quotients (1.0/0.75/0.5) and scale-100 compound extreme (100-char product + name, 99.99999999999999 score); score boundary just inside/outside (0.9999999999999999, 5.000000000000001, 100.00000000000001); 1e308 → V4 not V2 (finiteness passes, range first — order pinned); −0.0 score → V4; **weight −0.0 accepted** (same real as 0.0; results bit-equal to weight 0.0); 1e-300 weight accepted (contribution exactly 2e-301, not clamped to 0); 1.0000000000000002 → V5; −1e-12 → V5; **100 × 0.01 weights** (IEEE-754 sum 1.0000000000000007 ≠ 1.0, within EPSILON, accepted, never "fixed"); full 65-character control-set sweep (C0 32 + DEL 1 + C1 32) for names (V10) and product (V18) + non-control Unicode accepted; **Unicode casefold duplicate pinned** (Straße/STRASSE → V8); whitespace inside names not normalized ("a b" vs "a  b" distinct; " A B " still V8); group-weight corners ((1,0,0) etc.) + value-rules-precede-sum ordering + decimal group weights with exact-1.0 sum; **TV6 through the full pipeline** (C1-tolerated weights: indices > 1.0 and ≤ 1+EPSILON, overall ≤ 1+2·EPSILON, gap 0.0 — no clamping end-to-end).
- **Tests executed:** full suite `python3 -m unittest discover -s tests` → **215 tests, all passing** (58 domain + 40 calculation + 69 validation + 14 integration + 9 property + 25 edge-case).
- **Affected test vectors:** TV7 registered (new, additive — TV1–TV6 unchanged). No F1–F4 value changed; no formula changed.
- **Approval:** Explicit user task directive (2026-08-26, QA audit) — "audit the current implementation against the documentation", create unit/integration/regression/edge-case tests and mathematical test vectors, test the ten listed areas + property invariants, "do not modify production code merely to make tests pass", "every fixed bug requires a regression test". No behavioral bug was found; therefore **no regression tests were required** — instead the new edge-case tests *pin* current contract behaviors (the listed pinned behaviors in that module's docstring), so any future silent change fails loudly. Stale-claim fixes are documentation-only (no behavior diff; full suite green before and after).

## 2026-08-25 — Validation layer implemented (APPLICATION band); product name + score minimum registered

- **Type:** implementation (validation) + validation contract (V4/V5 amended, V16–V21 added) + domain (one new field) + assumption
- **Change:** Implemented the validation layer exactly per task scope — no UI, no Streamlit, no services, no diagnostics. `universality/validation.py`: `validate(raw) -> Evaluation` — the single authoritative user-input gate (raw dict with `product` / `scale_max` / `parameters`[name/weight/scores{fap,sap,dap}]; unknown keys ignored; fail-fast, first violated rule wins, structural → domain order documented); `validate_group_weights(...)` — the single C2/C5 validator (V20/V21 + per-element rules) the application layer applies to the group weights it uses (Simple Mode: the constant, defensively re-validated on every call — M5 unchanged: group weights are still **not** user input); `ValidationRejection(code, field, message)` — frozen dataclass, `Exception` subclass, `str()` = user-facing message. Rules implemented per the amended V-table: product name (V16/V17/V18), names (V9/V10/V8, unique case-insensitive after trimming), weights (V3/V13/V1/V2/V5 per element; V6 sum 1±EPSILON — **a total of 0.96 is an error, never normalized**), scores (V14 coverage; V3/V13/V1/V2; V4 range **1..scale_max**), scale (V3/V11 int 2..100, booleans rejected), structural types (V19), group weights (V20/V21). NaN/±Infinity/booleans/None/negatives/oversized strings/duplicates all rejected with friendly field-named messages; no silent repair, trimming, rescaling, or imputation; no internal exception ever escapes (construction safety net → generic V-UNEXPECTED message). Domain change: `Evaluation` gained a required first field `product: str` (A21; same name invariants as parameter names via the single `_check_name` predicate, which gained a `field` label parameter); domain's private contract constants promoted public (`SCALE_MIN/MAX`, `NAME_MIN_LENGTH/MAX_LENGTH`, `PARAMETER_MIN/MAX`, `CONTROL_CHARS`) so the gate reuses the single locations. `universality/__init__.py`: re-exports the validation surface (`__all__` = 25). **V-table amendments (change control, explicit user instruction):** V4 now "score < 1 or score > scale_max" (was "score < 0") — assumption **A20** (user-input minimum 1; engine/formula domain `[0, scale_max]` C3 unchanged; TV3/TV5 remain valid engine-level vectors, not user-producible); V5 range made explicit 0..1 (aligned with C5/input limits); new codes **V16–V21**; **V15 remains reserved** for the export CSV-injection guard (2026-08-25 architecture entry). Documentation synchronized: `validation-and-security.md` (schema, amended table, order, group-weights section, limits, safety net), `DOMAIN_MODEL.md` (product field, public constants, status notes), `FORMULA_SPECIFICATION.md` (score-domain gate note, TV3 note), `METHODOLOGY.md` (MVP list, concept 5), `LIMITATIONS.md` (M5 scope note), `architecture.md` (§5/§7/§8/§9/§10; the §5 "may import" cell for `validation.py` aligned with the exhaustive §6 edge list — `validation → domain, calculation`), `README.md`, `tests/CALCULATION_TEST_VECTORS.md` (engine-level note).
- **Tests added:** `tests/test_validation.py` — 69 tests: one negative + one positive case per V1–V21 (V15 reserved, no rule); the mandated cases 0.96 weight sum rejected (parameters and group weights), score 0 rejected (A20), NaN/±Infinity/True/False/None/negative/oversized/duplicate all rejected; EPSILON boundary (sum 1±5e-10 accepted, 1±5e-8 rejected); first-rejection-wins ordering (8 ordering tests); no-silent-fixing (names stored as provided, totals not rescaled, missing scores not zero-filled); no-internal-exposure (19-case adversarial battery: only `ValidationRejection` escapes, no forbidden substrings in messages); module purity (AST import whitelist per §6, forbidden-identifier scan, no formula bodies). `tests/test_domain.py` +3 product-invariant tests; `tests/test_calculation.py` construction sites updated for the new required field. **Total: 167 tests, all passing** (`python3 -m unittest discover -s tests`).
- **Affected test vectors:** TV1–TV6 unchanged as engine vectors (TV3/TV5 now documented as engine-level; not user-producible since A20). No F1–F4 value changed; no formula changed.
- **Approval:** Explicit user task directive (2026-08-25, validation task) enumerating the PRODUCT/PARAMETERS/WEIGHTS/SCORES/SCALE rules, the rejection list, and "if weights total 0.96, return an error". Flagged contract decisions: **A20** (score minimum 1 — amends V4, engine domain unchanged) and **A21** (required product name — new domain field + V16–V18); group-weight sum rule implemented as a defensive application-gate validator (M5 not opened).

## 2026-08-25 — Calculation engine implemented (CALCULATION layer); F4 registered

- **Type:** formula (F4 addition) + implementation (CALCULATION layer)
- **Change:** Implemented the CALCULATION layer exactly per task scope — no UI, no Streamlit, no diagnostics, no export, no other layers. `universality/calculation.py`: `SIMPLE_MODE_GROUP_WEIGHTS` (A1, sole location, 1/3 each); `normalize_score` [F1, sole implementation, F1 project-domain re-asserted as boundary guard]; `parameter_contributions` (F2 summands `w(i)·s_norm(i, G)`); `group_index` [F2]; `overall_index` [F3, C2 enforced on the engine surface via the single `domain.weights_sum_is_valid` predicate]; `group_gap` [F4, sole implementation]; `compute` (top-level; composes `overall_index` + `group_gap`, never re-derives; returns `EvaluationResult`). Guarantees, all test-enforced: pure and deterministic (fixed summation order, no I/O/global state); full IEEE-754 precision with **no rounding** (A5); **no clamping** of valid values (TV6); no imputation/inference; bit-exact structural identity `UI_G == Σᵢ contribution(i, G)`; single implementation per formula; imports limited to `math`, `typing`, `universality.domain` (AST-enforced). No pandas/numpy — documented reason: stdlib arithmetic is sufficient for ≤ 100 terms (dependency policy step 2). `universality/domain.py`: `EvaluationResult` gained `group_gap` (4th field; finite, ≥ 0 invariant) — the only domain change. `universality/__init__.py`: re-exports both layers (`__all__` = 22 members). **F4 registered per explicit user instruction** ("user-group gap", item 7 of the task directive) with interpretation A19 (max−min spread of the three group indices); F1–F3 text unchanged. Documentation: `docs/CALCULATION_ENGINE.md` created (engine contract, guarantees, M5 note — the engine's `group_weights` parameter is the formula itself and does NOT open gate M5); `tests/CALCULATION_TEST_VECTORS.md` created (vector→test map + TV5 hand-verification worksheet); `docs/FORMULA_SPECIFICATION.md` updated (F4 block, surface sentence, TV1–TV4 extended with gap expectations, TV5/TV6 added); `docs/DOMAIN_MODEL.md` (`EvaluationResult.group_gap` field row; surface sentence), `docs/METHODOLOGY.md` (concept 13 + §3), `docs/ASSUMPTIONS.md` (A19), `docs/architecture.md` (F1–F4 references; implementation status of DOMAIN/CALCULATION), `docs/LIMITATIONS.md` (M2 scope note), `README.md` synchronized; `domain.py` docstring and `tests/test_domain.py` comments updated for F4.
- **Tests added:** `tests/test_calculation.py` — 41 tests covering every mandated category: perfect scores, minimum scores, mixed scores, one parameter, many parameters (n = 100, two patterns), unequal parameter weights, unequal user-group weights, decimal weights, alternate Likert scales (3/7/100), invalid inputs (rejection, not clamping), floating-point tolerance (C1 boundary; why tolerances are required), plus the hand-verifiable TV5, TV6 no-clamping behavior, determinism (bit-identical repeat, input unmodified), and engine purity (import whitelist, forbidden-identifier scan). `tests/test_domain.py` updated for the new `group_gap` field. **Total: 95 tests, all passing** (`python3 -m unittest discover -s tests`).
- **Affected test vectors:** TV1–TV4 extended with `group_gap` expectations (no F1–F3 value change); TV5 (hand-verifiable reference case) and TV6 (no-clamping behavior) added.
- **Approval:** Explicit user task directive (2026-08-25) enumerating the seven computations, including "user-group gap" — this directive is the approval to add F4. F4's definition is recorded as A19 (Proposed, reviewable); F1–F3 unchanged.

## 2026-08-25 — Domain model implemented (layer: DOMAIN only)

- **Type:** domain
- **Change:** Implemented the DOMAIN layer exactly per task scope — no UI, no Streamlit, no diagnostics, no export, no other layers. `universality/domain.py`: `UserGroup` (FAP/SAP/DAP + labels, R2/R3), `KanoCategory` (inert 5-class vocabulary — M2 scope note: vocabulary approved by explicit user instruction, all Kano behavior remains gated), `Score`, `Weight`, `GroupScores`, `Parameter`, `Evaluation` (scale_max int 2–100; 1–100 parameters; A10 uniqueness; C1 weight sum via the single predicate; C3 score domain), `PerGroupValue`, `ParameterResult` (carries F1 outputs + F2 summands **as data** — no new formula), `EvaluationResult` (F2/F3 outputs; C4 range documented with tolerance bound, not over-asserted), `EvaluationOutcome` (single boundary object; count/order/name consistency invariants), `DomainInvariantError`, `weights_sum_is_valid` (single C1/C2 implementation), `EPSILON` (A5) — now located in `domain.py`, where the C1/C2 invariants are enforced (`SIMPLE_MODE_GROUP_WEIGHTS` remains in the planned `calculation.py` location, deferred). All object types are frozen dataclasses with all-required fields; no UI/framework imports (test-enforced); no formula bodies in the domain (test-enforced). `universality/__init__.py`: public API re-exporting the domain surface (APPLICATION re-exports deferred). `docs/DOMAIN_MODEL.md` created (field-level authority, invariant split, constraint→contract map); `docs/architecture.md` §5/§8, `README.md`, `docs/METHODOLOGY.md` (concept 10), `docs/LIMITATIONS.md` (M2) synchronized; assumption A18 added.
- **Tests added:** `tests/test_domain.py` — 54 tests covering every field's valid range, required-field status, immutability, the C1/C2 predicate, name rules, outcome consistency, and domain purity (stdlib-only imports, no formula bodies). **All 54 pass** (`python -m unittest discover -s tests`).
- **Affected test vectors:** TV1–TV4 unchanged (CALCULATION layer not started).
- **Approval:** User task directive 2026-08-25 ("implement ONLY the domain model"); Kano vocabulary type included per explicit user instruction, M2 gate for Kano behavior unchanged.

## 2026-08-25 — Production architecture finalized

- **Type:** architecture
- **Change:** `docs/architecture.md` upgraded from proposal to finalized production architecture: four-band model (UI → Application → Domain/Calculation → Infrastructure, strictly downward; Infrastructure reserved with zero MVP modules — A13/M8/no-DB rule); annotated directory tree; per-directory and per-module responsibility tables; exhaustive permitted-import-edge table with `ast`-based enforcement (`scripts/security_audit.py` + `tests/test_security_audit.py`, stdlib only); data flow (raw dict confined to the Application entry point; frozen domain data below); domain model (six frozen dataclasses incl. `EvaluationOutcome` as the single boundary object; `ValidationRejection`/`ServiceError`; single locations for `EPSILON` and `SIMPLE_MODE_GROUP_WEIGHTS`); error flow with redaction rule; testing architecture (stdlib `unittest`, mirror principle, seeded determinism, `1e-12` tolerance discipline); extension strategy (research lane `universality/research/`, pure `export.py` with mandatory CSV-injection guard, diagnostics graduation, M8-gated persistence with scoped import-policy exception, optional headless CLI); security boundaries (five trust zones; V15 CSV-injection guard **flagged as candidate for the export task**, not silently added). Reuse check performed: prior proposed modules (`domain`, `calculation`, `validation`, `services`, `tests/test_*`, `ui/app.py`, `__init__`) reused; only `diagnostics.py`, `test_services.py`, `export.py` (deferred), `research/` (deferred), and `scripts/security_audit.py` are new, each with documented purpose. No formula changed; F1–F3 remain owned by `FORMULA_SPECIFICATION.md` (referenced by ID only).
- **Affected test vectors:** TV1–TV4 unchanged; test-module responsibilities specified (no code yet).
- **Approval:** User architecture-design directive (2026-08-25): design only, no application code.

## 2026-08-25 — Research-methodology contract established (methodology audit)

- **Type:** documentation + assumption
- **Change:** The six-file research-methodology contract was established and audited: `RESEARCH_BASIS.md` (verified claims with [P-BRIEF]/[P-PUBLIC]/[P-UNVERIFIED] provenance, claim register), `METHODOLOGY.md` (12-concept methodology map; formal MVP and Research Mode definitions — Research Mode is a governed extension lane with **zero** approved features), `FORMULA_SPECIFICATION.md` (authoritative formulas F1–F3 with provenance; F1–F3 declared the entire calculation surface), `ASSUMPTIONS.md` (register A1–A15 carried forward unchanged + audit-derived A16 `max(S) := scale_max` and A17 single-scalar group scores), `LIMITATIONS.md` (claim/behavior prohibitions, must-NOT-implement M1–M10 with gates, ambiguities U1–U8 — all open), `REFERENCES.md` (bibliography incl. standard-literature Kano [M1] and AHP [M2], access status, verification log). Three earlier documents were superseded by redirect stubs (no content loss): `research-methodology.md` → `RESEARCH_BASIS.md`+`REFERENCES.md`, `computational-model.md` → `FORMULA_SPECIFICATION.md`, `application-assumptions.md` → `ASSUMPTIONS.md`. README document map, architecture single-source-of-truth, validation-and-security pointer, and AI guardrails were synchronized to the new set.
- **Audit result:** 12/12 concepts dispositioned (definition, mathematical role, MVP/Research Mode placement, optionality, user-visibility). Concepts 1–9 are fully specified by [P-PUBLIC] or [P-BRIEF] sources. Concepts 10–11 (Kano, AHP) are general literature only — [P-UNVERIFIED] as to this research — and are must-NOT-implement (M2, M1). No claim was promoted beyond its source; no formula was changed.
- **Affected test vectors:** TV1–TV4 unchanged (re-homed in `FORMULA_SPECIFICATION.md`).
- **Approval:** User audit-task directive (2026-08-25): establish the mathematical and methodological contract; mark anything not conclusively established UNVERIFIED.

## 2026-08-25 — Documentation foundation created

- **Type:** documentation
- **Change:** Initial documentation of the project: research claim register with sources and honest verification status; authoritative computational model (Simple Mode, student-declared scale, 1/3 group weights, 1/n parameter-weight default, constraints C1–C5, numerical rules, canonical test vectors TV1–TV4); assumption register A1–A15 (mandated / confirmed / proposed); layered architecture with hard boundaries and a proposed module layout; validation and security contract (V1–V14, forbidden constructs, input limits); AI guardrails. **No implementation exists yet** — the repository is in documentation-only status.
- **Affected test vectors:** TV1–TV4 defined (no code exists to exercise them yet).
- **Approval:** User scope decision "documentation foundation only" (2026-08-25); scale and parameter-model decisions confirmed by the user the same day (A2, A3).
